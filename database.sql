create TABLE person(
    id SERIAL PRIMARY KEY ,
    title varchar(255),
    description varchar(255)
);
create TABLE person(
    id SERIAL PRIMARY KEY,
    name varchar(255),
    surname varchar(255)
);
create TABLE person(
    id SERIAL PRIMARY KEY ,
    title varchar(255),
    description varchar(255),
    isDone boolean
);
title, description
